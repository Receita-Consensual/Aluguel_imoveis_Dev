cd ~/Aluguel_imoveis_Dev && python3 -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path.cwd() / 'motor_busca')); from motor_busca.config import SUPABASE_URL, SUPABASE_SERVICE_KEY; from motor_busca.db import get_supabase_client; supabase = get_supabase_client(SUPABASE_URL, SUPABASE_SERVICE_KEY); supabase.table('imoveis').select('id').limit(1).execute(); print('OK')"
